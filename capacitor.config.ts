import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourapp.RenderVerseapp',
  appName: 'RenderVerse',
  webDir: 'build'
};

export default config;
